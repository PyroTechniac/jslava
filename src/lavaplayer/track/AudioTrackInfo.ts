export class AudioTrackInfo {
    public constructor(public title: string, public author: string, public length: number, public identifier: string, public isStream: boolean, public uri: string) { }
}
